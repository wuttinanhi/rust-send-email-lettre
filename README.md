# rust-send-email-lettre

An example of sending an email using the Rust `lettre` library.

## Development

Run the setup script to install the necessary libraries:
```sh
./devsetup.sh
```

To run the project, use:
```sh
cargo run
```
